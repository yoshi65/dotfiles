# Only track specific .config directories
# Everything else in .config is ignored by default
.config/*
!.config/dein/
!.config/git/
!.config/nvim/
!.config/template/
!.config/flake8
!.config/pycodestyle
!.config/starship.toml

# Neovim: Exclude lazy.nvim lock file (contains git hashes)
.config/nvim/lazy-lock.json

# Claude Code runtime data (contains session IDs)
.config/claude/

# Runtime directories and caches
.tmux/plugins/
.tmux/resurrect/
logs/
iterm2/

# Sensitive files patterns (extra safety)
**/*credentials*
**/*token*
**/*.log
**/*.db
